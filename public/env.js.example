/**
 * Readcast Runtime Configuration Template
 * 
 * To use this, copy this file to 'public/env.js' and modify the values below.
 * 'public/env.js' is loaded by 'index.html' before the main application logic.
 */

window.__READCAST_ENV__ = {
    /**
     * Optional: provide your own CORS proxy URL.
     * Supported formats:
     *   1) Query-param proxy: https://your-proxy.example.com/?url= (Readcast appends the encoded target URL)
     *   2) Template URL: https://your-proxy.example.com/?url={url} (Readcast replaces {url} with the encoded target URL)
     */
    // READCAST_CORS_PROXY_URL: 'https://api.allorigins.win',

    /**
     * If true, try READCAST_CORS_PROXY_URL first and fall back to public proxies.
     * If false, use public proxies first and fall back to READCAST_CORS_PROXY_URL.
     */
    READCAST_CORS_PROXY_PRIMARY: false
};
