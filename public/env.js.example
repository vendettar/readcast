/**
 * Readio Runtime Configuration Template
 * 
 * To use this, copy this file to 'public/env.js' and modify the values below.
 * 'public/env.js' is loaded by 'index.html' before the main application logic.
 */

window.__READIO_ENV__ = window.__READIO_ENV__ || {
  /**
   * Primary CORS Proxy URL.
   * Format: Template (with {url}), Prefix (ends with ?url=), or Base URL.
   */
  READIO_CORS_PROXY_URL: 'https://api.allorigins.win/get?url=',

  /**
   * If true, always use proxy. If false, try direct fetch first.
   */
  READIO_CORS_PROXY_PRIMARY: false,
};
