## Readcast runtime config (served via `/env.js`)
##
## Optional: provide your own CORS proxy URL.
## Supported formats:
##   1) Query-param proxy (recommended): `https://your-proxy.example.com/?url=` (Readcast appends the encoded target URL)
##   2) Template URL: `https://your-proxy.example.com/?url={url}` (Readcast replaces `{url}` with the encoded target URL)
##
## The proxy should return the raw response body from the target (XML/JSON/text).
##
## Examples:
## READCAST_CORS_PROXY_URL=https://api.allorigins.win
## READCAST_CORS_PROXY_URL=https://itunes-proxy.example.com/?url=
READCAST_CORS_PROXY_URL=

## If `true`, try `READCAST_CORS_PROXY_URL` first and fall back to allorigins.
## If `false`, use allorigins first and fall back to `READCAST_CORS_PROXY_URL`.
READCAST_CORS_PROXY_PRIMARY=false
